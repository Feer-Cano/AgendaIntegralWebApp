import { AccessLevelDetails } from './access-level-details';

describe('AccessLevelDetails', () => {
  it('should create an instance', () => {
    expect(new AccessLevelDetails()).toBeTruthy();
  });
});
